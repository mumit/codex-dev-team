# Tiny App Example

A minimal Node project used to dogfood `codex-dev-team` installation and
pipeline commands against a real target layout.

```bash
npm test
bash ../../bootstrap.sh .
npm run pipeline:scaffold -- "Add health endpoint"
```

